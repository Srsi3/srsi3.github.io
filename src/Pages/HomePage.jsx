//sliding cubes that act like links, resume, linkedin, & github links at top
import React from 'react';
import { Canvas } from '@react-three/fiber';
const HomePage = () =>{
    //example link format for later
return(
    <div>
      <a href="https://www..com" target="_blank" rel="noopener noreferrer">
        Visit Example.com
      </a>
    </div>
);
}